# App Tour — tutoriel guidé multi-pages — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un onboarding interactif : modale de bienvenue à la 1ʳᵉ connexion proposant un « app tour » multi-pages avec spotlight (arrière-plan flouté) qui met en avant chaque feature, skippable, relançable depuis Paramètres → Accessibilité → Tutoriel.

**Architecture :** App Next.js 16 (App Router), hexagonale côté serveur. La persistance « tour vu » passe par le domaine/application (test vitest). Le moteur du tour est un composant client custom (sans lib) monté dans `AppShell`, piloté par des étapes déclaratives ciblant des attributs `data-tour`.

**Tech Stack :** Next.js 16.2.9, React 19, Tailwind v4, TypeScript strict, MongoDB, vitest.

## Global Constraints

- Aucune mention IA / Claude / superpowers dans le code, l'app ou les commits ; aucun co-auteur Claude.
- Ne jamais toucher au dossier `lechatonfat/`.
- Architecture hexagonale : toute dépendance externe derrière un port ; le domaine ne connaît aucun driver/SDK.
- Aucun secret en dur ; validation des entrées aux frontières ; typage strict, pas de `any`.
- Commits fréquents, un par tâche, directement sur `master`.
- Vérifier avant de conclure : `npx tsc --noEmit` propre + `npx vitest run` vert.

## File Structure

- `src/server/domain/entities.ts` — `tourCompletedAt` sur `User`.
- `src/server/application/register-user.ts`, `src/server/adapters/mongo/mappers.ts` — défaut `null`.
- `src/server/application/tour.ts` (nouveau) — `markTourSeen`.
- `src/app/api/me/tour-seen/route.ts` (nouveau) — endpoint.
- `src/lib/tour-target.ts` (nouveau) — type `TourStep` + résolution pure `resolveTargetName`.
- `src/lib/tour-steps.ts` (nouveau) — `TOUR_STEPS`.
- `src/components/tour/TourProvider.tsx`, `TourOverlay.tsx`, `WelcomeModal.tsx` (nouveaux) — moteur + UI.
- `src/components/AppShell.tsx` — montage + `data-tour` sur la nav.
- `src/components/SearchPanel.tsx`, `src/app/bibliotheque/LibraryClient.tsx`, `src/app/communautes/CommunitiesClient.tsx`, `src/components/ProfileTabs.tsx` — cibles `data-tour`.
- `src/app/parametres/SettingsClient.tsx` — section Accessibilité.

---

## Task 1 : Persistance « tour vu »

**Files:**
- Modify: `src/server/domain/entities.ts`, `src/server/application/register-user.ts`, `src/server/adapters/mongo/mappers.ts`
- Create: `src/server/application/tour.ts`, `src/app/api/me/tour-seen/route.ts`
- Test: `tests/application/tour.test.ts`

**Interfaces:**
- Produces: `User.tourCompletedAt: Date | null` ; `markTourSeen(deps: Deps, userId: string): Promise<User>`.

- [ ] **Step 1 : Champ domaine**

Dans `src/server/domain/entities.ts`, dans `interface User`, ajouter après `notificationsSeenAt: Date | null;` :

```ts
  tourCompletedAt: Date | null;    // null = app tour jamais vu/complété
```

- [ ] **Step 2 : Défaut à l'inscription + mapper Mongo**

Dans `src/server/application/register-user.ts`, dans l'objet utilisateur créé, ajouter après `notificationsSeenAt: null,` :

```ts
    tourCompletedAt: null,
```

Dans `src/server/adapters/mongo/mappers.ts`, dans `fromUserDoc`, ajouter le défaut rétrocompatible (à côté de `twoFactorSecret: d.twoFactorSecret ?? null,`) :

```ts
  tourCompletedAt: d.tourCompletedAt ?? null,
```

- [ ] **Step 3 : Test du use-case (échoue)**

Créer `tests/application/tour.test.ts` :

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { makeInMemoryDeps, type Deps } from "@/server/container";
import { FakeCatalog } from "../helpers/fake-catalog";
import { registerUser } from "@/server/application/register-user";
import { markTourSeen } from "@/server/application/tour";

let deps: Deps; let uid: string;
const tastes = { filmGenreIds: [878, 18, 35], people: [] };
beforeEach(async () => {
  deps = makeInMemoryDeps(new FakeCatalog());
  uid = (await registerUser(deps, { email: "u@x.io", username: "u", displayName: "U", password: "password1", activeTabs: ["films"], tastes })).id;
});

describe("markTourSeen", () => {
  it("pose tourCompletedAt et le persiste", async () => {
    const before = await deps.users.findById(uid);
    expect(before?.tourCompletedAt).toBeNull();
    const updated = await markTourSeen(deps, uid);
    expect(updated.tourCompletedAt).not.toBeNull();
    const reloaded = await deps.users.findById(uid);
    expect(reloaded?.tourCompletedAt).not.toBeNull();
  });
  it("est idempotent (réappel ne casse rien)", async () => {
    await markTourSeen(deps, uid);
    const again = await markTourSeen(deps, uid);
    expect(again.tourCompletedAt).not.toBeNull();
  });
});
```

- [ ] **Step 4 : Lancer → échoue**

Run: `npx vitest run tests/application/tour.test.ts`
Expected: FAIL (module `tour` introuvable / champ absent).

- [ ] **Step 5 : Implémenter le use-case**

Vérifier la méthode de mise à jour utilisateur du port (`grep -n "update\|upsert\|save" src/server/ports/repositories.ts | head` — la `UserRepository` expose `update` ou `save`). Créer `src/server/application/tour.ts` en utilisant la méthode existante (ci-dessous `deps.users.update` ; si le port expose un autre nom, l'adapter) :

```ts
import type { Deps } from "@/server/container";
import type { User } from "@/server/domain/entities";
import { NotFoundError } from "@/server/domain/errors";

/** Marque l'app tour comme vu/complété pour l'utilisateur (idempotent). */
export async function markTourSeen(deps: Deps, userId: string): Promise<User> {
  const user = await deps.users.findById(userId);
  if (!user) throw new NotFoundError("Utilisateur introuvable");
  const updated: User = { ...user, tourCompletedAt: deps.clock.now() };
  await deps.users.update(updated);
  return updated;
}
```

Note : confirmer le nom exact de la méthode de persistance (`update` vs `save` vs `upsert`) via le grep ci-dessus et l'utiliser tel quel. Les autres use-cases mutant un `User` (ex. `setAccountPrivacy` dans `social.ts`) montrent la méthode à employer — s'aligner dessus.

- [ ] **Step 6 : Test → passe**

Run: `npx vitest run tests/application/tour.test.ts`
Expected: PASS.

- [ ] **Step 7 : Endpoint**

Créer `src/app/api/me/tour-seen/route.ts` :

```ts
import { getDeps } from "@/server/container";
import { markTourSeen } from "@/server/application/tour";
import { requireUser, json } from "@/server/http/session";
import { toErrorResponse } from "@/server/http/errors";
import { selfUser } from "@/lib/serialize";

export async function POST() {
  try {
    const user = await requireUser();
    return json({ user: selfUser(await markTourSeen(await getDeps(), user.id)) });
  } catch (e) { return toErrorResponse(e); }
}
```

(`selfUser` renvoie déjà tout le user hors secrets — `tourCompletedAt` inclus via le spread.)

- [ ] **Step 8 : Vérifier + commit**

Run: `npx tsc --noEmit && npx vitest run`
Expected: propre + vert.

```bash
git add src/server/domain/entities.ts src/server/application/register-user.ts src/server/adapters/mongo/mappers.ts src/server/application/tour.ts src/app/api/me/tour-seen/route.ts tests/application/tour.test.ts
git commit -m "feat(vulu): persistance de l'app tour (tourCompletedAt + markTourSeen + endpoint)"
```

---

## Task 2 : Étapes du tour + résolution de cible (pur, testé)

**Files:**
- Create: `src/lib/tour-target.ts`, `src/lib/tour-steps.ts`
- Test: `tests/lib/tour-target.test.ts`

**Interfaces:**
- Produces: `TourStep` (type), `resolveTargetName(step, isPresent): string | null`, `TOUR_STEPS: TourStep[]`.

- [ ] **Step 1 : Type + résolution pure**

Créer `src/lib/tour-target.ts` :

```ts
export type TourPlacement = "top" | "bottom" | "left" | "right" | "center";

export interface TourStep {
  id: string;
  route: string;
  target: string;        // valeur data-tour de la cible principale
  fallbackTarget?: string; // valeur data-tour de repli (ex. item de nav)
  title: string;
  body: string;
  placement: TourPlacement;
}

/** Nom de data-tour à spotlighter : cible → repli → null (tooltip centrée). Pur. */
export function resolveTargetName(step: TourStep, isPresent: (name: string) => boolean): string | null {
  if (step.placement === "center") return null;
  if (isPresent(step.target)) return step.target;
  if (step.fallbackTarget && isPresent(step.fallbackTarget)) return step.fallbackTarget;
  return null;
}
```

- [ ] **Step 2 : Test (échoue)**

Créer `tests/lib/tour-target.test.ts` :

```ts
import { describe, it, expect } from "vitest";
import { resolveTargetName, type TourStep } from "@/lib/tour-target";

const step: TourStep = { id: "s", route: "/feed", target: "feed-card", fallbackTarget: "nav-feed", title: "t", body: "b", placement: "bottom" };

describe("resolveTargetName", () => {
  it("cible présente → cible", () => {
    expect(resolveTargetName(step, (n) => n === "feed-card")).toBe("feed-card");
  });
  it("cible absente, repli présent → repli", () => {
    expect(resolveTargetName(step, (n) => n === "nav-feed")).toBe("nav-feed");
  });
  it("les deux absents → null", () => {
    expect(resolveTargetName(step, () => false)).toBeNull();
  });
  it("placement center → null", () => {
    expect(resolveTargetName({ ...step, placement: "center" }, () => true)).toBeNull();
  });
});
```

- [ ] **Step 3 : Lancer → échoue**

Run: `npx vitest run tests/lib/tour-target.test.ts`
Expected: FAIL (module introuvable).

- [ ] **Step 4 : Lancer → passe (la lib du Step 1 satisfait le test)**

Run: `npx vitest run tests/lib/tour-target.test.ts`
Expected: PASS.

- [ ] **Step 5 : Liste des étapes**

Créer `src/lib/tour-steps.ts` :

```ts
import type { TourStep } from "./tour-target";

export const TOUR_STEPS: TourStep[] = [
  { id: "feed", route: "/feed", target: "feed-card", fallbackTarget: "nav-feed", placement: "bottom",
    title: "Ton feed", body: "Les avis et activités de ton cercle apparaissent ici." },
  { id: "search", route: "/search", target: "search-input", fallbackTarget: "nav-search", placement: "bottom",
    title: "Rechercher", body: "Trouve un film, une série, un livre… ou un membre avec @pseudo." },
  { id: "library", route: "/bibliotheque", target: "create-collection", fallbackTarget: "nav-library", placement: "bottom",
    title: "Ta bibliothèque", body: "Organise tes œuvres en collections, et suis ce que tu as vu / à voir." },
  { id: "communities", route: "/communautes", target: "community-cta", fallbackTarget: "nav-communities", placement: "bottom",
    title: "Communautés", body: "Rejoins des communautés autour de tes goûts (création réservée à vulu+)." },
  { id: "profile", route: "/u/me", target: "profile-showcase", fallbackTarget: "nav-profile", placement: "top",
    title: "Ton profil", body: "Mets en avant tes œuvres cultes dans ta vitrine." },
  { id: "end", route: "/feed", target: "center", placement: "center",
    title: "Tu es prêt·e ✨", body: "Tu pourras relancer ce tour à tout moment depuis Paramètres → Accessibilité." },
];
```

- [ ] **Step 6 : Vérifier + commit**

Run: `npx tsc --noEmit && npx vitest run tests/lib/tour-target.test.ts`
Expected: propre + vert.

```bash
git add src/lib/tour-target.ts src/lib/tour-steps.ts tests/lib/tour-target.test.ts
git commit -m "feat(vulu): app tour — étapes déclaratives + résolution de cible (pur, testé)"
```

---

## Task 3 : Attributs `data-tour` sur les cibles

**Files:**
- Modify: `src/components/AppShell.tsx`, `src/components/SearchPanel.tsx`, `src/app/bibliotheque/LibraryClient.tsx`, `src/app/communautes/CommunitiesClient.tsx`, `src/components/ProfileTabs.tsx`

- [ ] **Step 1 : Nav desktop + mobile (AppShell)**

Dans `src/components/AppShell.tsx`, étendre les tableaux de nav avec un champ `tour`. Remplacer la déclaration `const NAV: ...` par (ajouter `tour?: string` au type et les valeurs) :

```tsx
const NAV: { href: string; label: string; icon: IconName; pulse?: boolean; tour?: string }[] = [
  { href: "/feed", label: "Feed", icon: "home", pulse: true, tour: "nav-feed" },
  { href: "/search", label: "Recherche", icon: "search", tour: "nav-search" },
  { href: "/notifications", label: "Notifications", icon: "bell" },
  { href: "/communautes", label: "Communautés", icon: "community", tour: "nav-communities" },
  { href: "/bibliotheque", label: "Bibliothèque", icon: "lists", tour: "nav-library" },
  { href: "/u/me", label: "Profil", icon: "profile", tour: "nav-profile" },
  { href: "/plus", label: "vulu+", icon: "star" },
  { href: "/parametres", label: "Paramètres", icon: "settings" },
];
```

et le `BOTTOM_NAV` (ajouter `tour?: string` au type + valeurs correspondantes) :

```tsx
const BOTTOM_NAV: { href: string; label: string; icon: IconName; pulse?: boolean; tour?: string }[] = [
  { href: "/feed", label: "Accueil", icon: "home", pulse: true, tour: "nav-feed" },
  { href: "/communautes", label: "Communautés", icon: "community", tour: "nav-communities" },
  { href: "/bibliotheque", label: "Bibliothèque", icon: "lists", tour: "nav-library" },
  { href: "/notifications", label: "Notifications", icon: "bell" },
];
```

Puis, sur le `<Link>` de la nav desktop (`{NAV.map((n) => (`), ajouter l'attribut `data-tour={n.tour}` :

```tsx
          <Link key={n.href} href={n.href} data-tour={n.tour}
```

Et faire de même sur le `<Link>` de la barre mobile (`{BOTTOM_NAV.map(...)}`) : ajouter `data-tour={n.tour}` sur son `<Link>`.

- [ ] **Step 2 : Champ de recherche (SearchPanel)**

Dans `src/components/SearchPanel.tsx`, sur l'`<input>` de recherche (celui en `w-full min-w-0 rounded-full …`), ajouter `data-tour="search-input"`.

- [ ] **Step 3 : Bouton « Collection » (LibraryClient)**

Dans `src/app/bibliotheque/LibraryClient.tsx`, sur le bouton d'en-tête « Collection » (`onClick={() => setCreating(true)}`), ajouter `data-tour="create-collection"`.

- [ ] **Step 4 : CTA communauté (CommunitiesClient)**

Dans `src/app/communautes/CommunitiesClient.tsx`, ajouter `data-tour="community-cta"` sur l'élément d'en-tête présent dans les deux cas : le bouton « Créer » (plus) et le `<Link>` « Créer · vulu+ » (non-plus). Concrètement, ajouter l'attribut sur **les deux** éléments du ternaire `{plus ? <button …> : <Link …>}` pour que la cible existe quel que soit le statut.

- [ ] **Step 5 : Vitrine (ProfileTabs)**

Dans `src/components/ProfileTabs.tsx`, sur le conteneur de l'onglet Vitrine — repérer le bloc `render` de l'onglet `id: "vitrine"` (`<div>` racine du rendu) — ajouter `data-tour="profile-showcase"` sur ce `<div>`.

- [ ] **Step 6 : Vérifier + commit**

Run: `npx tsc --noEmit`
Expected: aucune sortie.

```bash
git add src/components/AppShell.tsx src/components/SearchPanel.tsx src/app/bibliotheque/LibraryClient.tsx src/app/communautes/CommunitiesClient.tsx src/components/ProfileTabs.tsx
git commit -m "feat(vulu): app tour — attributs data-tour sur les cibles (nav, recherche, collection, communauté, vitrine)"
```

---

## Task 4 : Moteur du tour (provider + overlay + modale de bienvenue)

**Files:**
- Create: `src/components/tour/TourProvider.tsx`, `src/components/tour/TourOverlay.tsx`, `src/components/tour/WelcomeModal.tsx`
- Modify: `src/components/AppShell.tsx`

**Interfaces:**
- Consumes: `TOUR_STEPS` (Task 2), `resolveTargetName` (Task 2), `POST /api/me/tour-seen` (Task 1), `data-tour` cibles (Task 3).
- Produces: `useTour(): { startTour: () => void }` (contexte, consommé par Task 5).

- [ ] **Step 1 : Overlay spotlight**

Créer `src/components/tour/TourOverlay.tsx` :

```tsx
"use client";
import { useEffect, useState } from "react";
import type { TourStep } from "@/lib/tour-target";

interface Rect { top: number; left: number; width: number; height: number }

function readRect(el: Element | null): Rect | null {
  if (!el) return null;
  const r = el.getBoundingClientRect();
  if (r.width === 0 && r.height === 0) return null;
  return { top: r.top, left: r.left, width: r.width, height: r.height };
}

export function TourOverlay({ step, index, total, targetEl, onPrev, onNext, onSkip }: {
  step: TourStep; index: number; total: number; targetEl: Element | null;
  onPrev: () => void; onNext: () => void; onSkip: () => void;
}) {
  const [rect, setRect] = useState<Rect | null>(() => readRect(targetEl));

  useEffect(() => {
    const update = () => setRect(readRect(targetEl));
    update();
    const t = setTimeout(update, 250); // laisser le layout se stabiliser
    window.addEventListener("resize", update);
    window.addEventListener("scroll", update, true);
    return () => { clearTimeout(t); window.removeEventListener("resize", update); window.removeEventListener("scroll", update, true); };
  }, [targetEl]);

  const pad = 8;
  const hole = rect ? { top: rect.top - pad, left: rect.left - pad, width: rect.width + pad * 2, height: rect.height + pad * 2 } : null;
  const panel = "fixed bg-black/55 backdrop-blur-sm";

  // Position de la tooltip (sous ou au-dessus de la cible ; centrée si pas de cible).
  const tipStyle: React.CSSProperties = hole
    ? step.placement === "top"
      ? { position: "fixed", left: Math.max(12, Math.min(hole.left, window.innerWidth - 340)), top: Math.max(12, hole.top - 12), transform: "translateY(-100%)" }
      : { position: "fixed", left: Math.max(12, Math.min(hole.left, window.innerWidth - 340)), top: hole.top + hole.height + 12 }
    : { position: "fixed", left: "50%", top: "50%", transform: "translate(-50%, -50%)" };

  return (
    <div className="fixed inset-0 z-[80]">
      {hole ? (
        <>
          <div className={panel} style={{ top: 0, left: 0, width: "100vw", height: Math.max(0, hole.top) }} />
          <div className={panel} style={{ top: hole.top + hole.height, left: 0, width: "100vw", height: `calc(100vh - ${hole.top + hole.height}px)` }} />
          <div className={panel} style={{ top: hole.top, left: 0, width: Math.max(0, hole.left), height: hole.height }} />
          <div className={panel} style={{ top: hole.top, left: hole.left + hole.width, width: `calc(100vw - ${hole.left + hole.width}px)`, height: hole.height }} />
          <div className="pointer-events-none fixed rounded-xl ring-2 ring-[var(--color-primary)] shadow-[0_0_0_4px_color-mix(in_srgb,var(--color-primary)_35%,transparent)]"
            style={{ top: hole.top, left: hole.left, width: hole.width, height: hole.height }} />
        </>
      ) : (
        <div className="fixed inset-0 bg-black/60 backdrop-blur-sm" />
      )}

      <div style={tipStyle} className="w-[min(20rem,calc(100vw-24px))] rounded-2xl border border-[var(--color-border)] bg-[var(--color-surface)] p-4 shadow-2xl">
        <div className="mb-2 flex items-center justify-between">
          <span className="text-xs font-semibold text-[var(--color-text-muted)]">Étape {index + 1}/{total}</span>
          <button onClick={onSkip} className="text-xs font-semibold text-[var(--color-text-muted)] hover:text-[var(--color-text)]">Passer</button>
        </div>
        <p className="font-display text-lg font-bold">{step.title}</p>
        <p className="mt-1 text-sm text-[var(--color-text)]">{step.body}</p>
        <div className="mt-3 flex items-center gap-2">
          <button onClick={onPrev} disabled={index === 0}
            className="rounded-full border border-[var(--color-border)] px-4 py-1.5 text-sm font-semibold disabled:opacity-40">Précédent</button>
          <button onClick={onNext}
            className="ml-auto rounded-full bg-[var(--color-primary)] px-5 py-1.5 text-sm font-bold text-white active:scale-[0.98]">
            {index === total - 1 ? "Terminer" : "Suivant"}
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2 : Modale de bienvenue**

Créer `src/components/tour/WelcomeModal.tsx` :

```tsx
"use client";

export function WelcomeModal({ onStart, onSkip }: { onStart: () => void; onSkip: () => void }) {
  return (
    <div className="fixed inset-0 z-[85] flex items-center justify-center bg-black/50 p-4 backdrop-blur-sm">
      <div className="w-[min(26rem,100%)] overflow-hidden rounded-3xl border border-[var(--color-border)] bg-[var(--color-surface)] shadow-2xl">
        <div className="bg-gradient-to-br from-[var(--color-primary)] to-[var(--color-accent)] p-6 text-white">
          <p className="font-display text-3xl font-black leading-tight">Bienvenue sur vulu 👋</p>
          <p className="mt-2 text-sm text-white/90">Ton réseau pour partager films, séries et livres. On te montre comment ça marche ?</p>
        </div>
        <div className="flex flex-col gap-2 p-5">
          <button onClick={onStart} className="w-full rounded-full bg-[var(--color-primary)] py-3 text-base font-bold text-white active:scale-[0.98]">Faire le tour ✨</button>
          <button onClick={onSkip} className="w-full rounded-full border border-[var(--color-border)] py-3 text-sm font-semibold text-[var(--color-text-muted)] hover:text-[var(--color-text)]">Explorer seul</button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3 : Provider (moteur)**

Créer `src/components/tour/TourProvider.tsx` :

```tsx
"use client";
import { createContext, useContext, useCallback, useEffect, useRef, useState } from "react";
import { usePathname, useRouter } from "next/navigation";
import { api } from "@/lib/api-client";
import { TOUR_STEPS } from "@/lib/tour-steps";
import { resolveTargetName } from "@/lib/tour-target";
import { TourOverlay } from "./TourOverlay";
import { WelcomeModal } from "./WelcomeModal";

interface TourCtx { startTour: () => void }
const Ctx = createContext<TourCtx>({ startTour: () => {} });
export const useTour = () => useContext(Ctx);

function isPresent(name: string): boolean {
  const el = document.querySelector(`[data-tour="${name}"]`);
  if (!el) return false;
  const r = el.getBoundingClientRect();
  return r.width > 0 || r.height > 0;
}

/** Attend qu'une cible data-tour soit visible (borné). */
function waitForTarget(name: string, timeoutMs = 3000): Promise<Element | null> {
  return new Promise((resolve) => {
    const start = Date.now();
    const tick = () => {
      const el = document.querySelector(`[data-tour="${name}"]`);
      if (el) { const r = el.getBoundingClientRect(); if (r.width > 0 || r.height > 0) return resolve(el); }
      if (Date.now() - start > timeoutMs) return resolve(null);
      setTimeout(tick, 100);
    };
    tick();
  });
}

export function TourProvider({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();
  const router = useRouter();
  const [welcome, setWelcome] = useState(false);
  const [running, setRunning] = useState(false);
  const [index, setIndex] = useState(0);
  const [targetEl, setTargetEl] = useState<Element | null>(null);
  const shownWelcome = useRef(false);

  // Afficher la modale de bienvenue une fois, à la 1ʳᵉ arrivée sur /feed si tour jamais vu.
  useEffect(() => {
    if (shownWelcome.current || pathname !== "/feed") return;
    shownWelcome.current = true;
    api.get<{ user: { tourCompletedAt: string | null } }>("/api/me")
      .then((d) => { if (d.user.tourCompletedAt === null) setWelcome(true); })
      .catch(() => {});
  }, [pathname]);

  const markSeen = useCallback(() => { api.post("/api/me/tour-seen").catch(() => {}); }, []);

  // Charge l'étape courante : navigue si besoin, attend la cible, la résout.
  const loadStep = useCallback(async (i: number) => {
    const step = TOUR_STEPS[i];
    if (!step) return;
    if (step.placement === "center") { setTargetEl(null); return; }
    if (pathname !== step.route) router.push(step.route);
    setTargetEl(null);
    const name = await waitForTarget(step.target).then((el) => (el ? step.target : (step.fallbackTarget && (await waitForTarget(step.fallbackTarget, 800)) ? step.fallbackTarget : null)));
    // resolveTargetName garde la logique pure ; ici on a déjà attendu, on relit l'état courant.
    const finalName = name ?? resolveTargetName(step, isPresent);
    setTargetEl(finalName ? document.querySelector(`[data-tour="${finalName}"]`) : null);
  }, [pathname, router]);

  const startTour = useCallback(() => { setWelcome(false); setRunning(true); setIndex(0); void loadStep(0); }, [loadStep]);
  const finish = useCallback(() => { setRunning(false); markSeen(); }, [markSeen]);
  const next = useCallback(() => {
    setIndex((i) => {
      if (i >= TOUR_STEPS.length - 1) { finish(); return i; }
      const ni = i + 1; void loadStep(ni); return ni;
    });
  }, [finish, loadStep]);
  const prev = useCallback(() => { setIndex((i) => { const pi = Math.max(0, i - 1); void loadStep(pi); return pi; }); }, [loadStep]);
  const skip = useCallback(() => { setRunning(false); markSeen(); }, [markSeen]);

  const step = TOUR_STEPS[index];

  return (
    <Ctx.Provider value={{ startTour }}>
      {children}
      {welcome && <WelcomeModal onStart={startTour} onSkip={() => { setWelcome(false); markSeen(); }} />}
      {running && step && (
        <TourOverlay step={step} index={index} total={TOUR_STEPS.length} targetEl={targetEl}
          onPrev={prev} onNext={next} onSkip={skip} />
      )}
    </Ctx.Provider>
  );
}
```

Note : la ligne `.then((el) => (el ? … await …))` doit être une fonction `async`. Remplacer la résolution du nom par ce bloc explicite (plus lisible et correct) :

```tsx
    let finalName: string | null = null;
    const primary = await waitForTarget(step.target);
    if (primary) finalName = step.target;
    else if (step.fallbackTarget) { const fb = await waitForTarget(step.fallbackTarget, 800); if (fb) finalName = step.fallbackTarget; }
    setTargetEl(finalName ? document.querySelector(`[data-tour="${finalName}"]`) : null);
```

(Supprimer la version en une ligne ci-dessus ; garder uniquement ce bloc. `resolveTargetName`/`isPresent` restent utilisés/testés par la Task 2 ; on peut aussi appeler `resolveTargetName` en repli synchrone si les deux `waitForTarget` échouent, mais ce n'est pas nécessaire.)

- [ ] **Step 4 : Monter le provider dans AppShell**

Dans `src/components/AppShell.tsx`, importer le provider en haut :

```tsx
import { TourProvider } from "@/components/tour/TourProvider";
```

Puis envelopper l'arbre retourné. Repérer `return (` et l'ouverture `<div className="mx-auto grid …">` : entourer tout le `<div>…</div>` racine par `<TourProvider> … </TourProvider>`. Concrètement, remplacer `return (\n    <div className="mx-auto grid` par `return (\n    <TourProvider>\n    <div className="mx-auto grid`, et la balise fermante racine `</div>` finale du composant par `</div>\n    </TourProvider>`. Vérifier l'équilibrage des balises avec `npx tsc --noEmit`.

- [ ] **Step 5 : Vérifier**

Run: `npx tsc --noEmit && npx vitest run`
Expected: propre + vert. Manuel : se connecter avec un compte n'ayant jamais vu le tour → à l'arrivée sur `/feed`, la modale de bienvenue s'affiche ; « Faire le tour » lance le spotlight qui parcourt les 6 étapes en naviguant entre les pages, avec arrière-plan flouté ; « Passer »/« Terminer » ferme et ne réapparaît plus au rechargement.

- [ ] **Step 6 : Commit**

```bash
git add src/components/tour/TourProvider.tsx src/components/tour/TourOverlay.tsx src/components/tour/WelcomeModal.tsx src/components/AppShell.tsx
git commit -m "feat(vulu): app tour — moteur spotlight custom + modale de bienvenue (multi-pages)"
```

---

## Task 5 : Paramètres → Accessibilité → Tutoriel

**Files:**
- Modify: `src/app/parametres/SettingsClient.tsx`

**Interfaces:**
- Consumes: `useTour()` (Task 4).

- [ ] **Step 1 : Ajouter la section + le contenu**

Dans `src/app/parametres/SettingsClient.tsx` :
- Importer le hook : `import { useTour } from "@/components/tour/TourProvider";` et récupérer `const { startTour } = useTour();` dans le composant.
- Ajouter le type de section : là où `type SectionId = …` est défini (`grep -n "type SectionId" src/app/parametres/SettingsClient.tsx`), ajouter `"accessibility"` à l'union.
- Ajouter l'entrée dans `SECTIONS` (après `display` par exemple) :

```tsx
  { id: "accessibility", label: "Accessibilité", icon: "shield", desc: "Tutoriel, aides" },
```

- Rendre le contenu de la section. Repérer comment les sections rendent leur contenu (`grep -n "section === \|cur ===\|case \"display\"" src/app/parametres/SettingsClient.tsx`) et ajouter, sur le même modèle que les autres sections, un bloc pour `"accessibility"` :

```tsx
        {cur === "accessibility" && (
          <div className="rounded-2xl border border-[var(--color-border)] p-4">
            <h2 className="mb-1 font-display text-lg font-bold">Tutoriel</h2>
            <p className="mb-3 text-sm text-[var(--color-text-muted)]">Revois le fonctionnement de l'app avec l'app tour guidé.</p>
            <button onClick={startTour} className="rounded-full bg-[var(--color-primary)] px-5 py-2.5 text-sm font-bold text-white active:scale-[0.98]">
              (Re)lancer l'app tour
            </button>
          </div>
        )}
```

Adapter `cur`/`section` au nom réel de la variable d'état de section (voir le grep). Si le rendu se fait via un `switch`/objet, suivre le même patron que `display`.

- [ ] **Step 2 : Vérifier**

Run: `npx tsc --noEmit`
Expected: aucune sortie. Manuel : Paramètres → Accessibilité → « (Re)lancer l'app tour » démarre le tour (même si déjà vu).

- [ ] **Step 3 : Commit**

```bash
git add src/app/parametres/SettingsClient.tsx
git commit -m "feat(vulu): paramètres — Accessibilité → relancer l'app tour"
```

---

## Self-Review (couverture spec)

| Demande utilisateur | Tâche |
|---|---|
| Modale de bienvenue à la 1ʳᵉ connexion | 1, 4 |
| Accepter / refuser (découvrir seul) | 4 |
| Tutoriel superposé multi-pages indiquant où cliquer + explications | 2, 3, 4 |
| Design premium + floutage d'arrière-plan | 4 |
| Skip possible | 4 |
| Relançable depuis Paramètres → Accessibilité → Tutoriel | 5 |

Toutes les demandes sont couvertes.

## Notes pour la session d'exécution

- Base de dev : MongoDB Atlas. Serveur : `npm run dev`.
- Task 1 : confirmer le nom exact de la méthode `UserRepository` (`update`/`save`/`upsert`) via grep et s'aligner sur un use-case mutant existant (ex. `setAccountPrivacy`).
- Task 4 : le moteur est vérifié manuellement (desktop + mobile réel). Le spotlight se replie proprement sur l'item de nav ou une tooltip centrée si une cible de page manque.
- Rétrocompat : les comptes existants (`tourCompletedAt` absent → `null`) verront la modale une fois au prochain passage sur `/feed`.
